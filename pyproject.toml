[project]
name = "searchmcp"
version = "0.1.0"
description = "Web Search MCP Server using FastMCP and DuckDuckGo"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=2.0.0",
    "ddgs>=7.0.0",
]

[tool.uv]
dev-dependencies = [
    "pytest>=8.0.0",
    "pytest-cov>=4.1.0",
]
